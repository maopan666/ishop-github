spring:
  cloud:
    config:
      uri: http://127.0.0.1:1299 #配置服务器222222222222
      label: master
      name: application_plat_server #github上面名称
      profile: ${spring.profiles.active} #环境:在达成jar包运行的时候,动态传入参数